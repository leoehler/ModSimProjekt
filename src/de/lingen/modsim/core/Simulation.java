package de.lingen.modsim.core;

public class Simulation {

    /**
     * hold the actual main loop to simulate everything
     * so wie beim DES werden hier die objecte durch simuliert.
     * die einzelnen obejekte werden vorher eingestellt (anzahl, szenario, strategien, ...)
     * @return next time step
     */
    public int simulate() {
        return 0;
    }
}
